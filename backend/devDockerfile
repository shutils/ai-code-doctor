FROM python:3.10.12-slim

WORKDIR /opt/app

COPY Pipfile* ./

RUN pip install pipenv && pipenv sync --system

ENTRYPOINT ["flask", "--app", "src/app", "run", "--host=0.0.0.0", "--port=5000", "--debug"]
